cmake_minimum_required(VERSION 3.30)
project(scuff)

if (SCUFF_BUILD_CLIENT)
	add_subdirectory(client)
endif()
if (SCUFF_BUILD_SBOX)
	add_subdirectory(sbox)
endif()
if (SCUFF_BUILD_SCANNER)
	add_subdirectory(scan)
endif()
if (SCUFF_BUILD_TEST_HOST)
	add_subdirectory(test-host)
endif()
